import React, { useState } from 'react';

// component to get ALL data from our created DB
const QueryEntryBox = () => {
  const [variable, func] = useState('');

  return(
    <div>
      this is query entry box
    </div>
  )
}

export default QueryEntryBox;